version: '3.8'

services:
  echocron:
    build: .
    container_name: echocron
    env_file:
      - .env
    extra_hosts:
      - "${PI_NAME}:${PI_IP}"
    volumes:
      - .:/app
      - ./output:/output
    environment:
      - TZ=America/New_York
    restart: unless-stopped
